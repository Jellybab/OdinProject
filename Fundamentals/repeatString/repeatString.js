const repeatString = function(string, amount) {
    let repeatingString = "";
    for( let i = 0; i = amount.length; i++){
        repeatString+= string;
    }
    return string;
}

module.exports = repeatString
